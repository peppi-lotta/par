% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/par.R
\name{calculate_standardized_bootstrap_ci}
\alias{calculate_standardized_bootstrap_ci}
\title{Calculate Standardized Bootstrap Confidence Interval}
\usage{
calculate_standardized_bootstrap_ci(
  type,
  data,
  exposure_col,
  outcome_col,
  standarisation_col,
  interval,
  sample_count
)
}
\arguments{
\item{type}{String, type of the measure to calculate. Possible values are
"par" (population attributable risk) and "paf" (population attributable fraction).}

\item{data}{Data frame containing the dataset.}

\item{exposure_col}{String, name of the exposure column in the dataset.}

\item{outcome_col}{String, name of the outcome column in the dataset.}

\item{standarisation_col}{String, name of the column used for standardization.}

\item{interval}{Numeric, interval for the confidence interval (default is 0.95).
Possible values are between 0 and 1.}

\item{sample_count}{Integer, number of bootstrap samples to draw (default is 10000).}
}
\value{
A matrix containing the lower and upper bounds of the standardized
confidence interval. The first column contains the lower bound, and the
second column contains the upper bound.
}
\description{
This function calculates the standardized confidence interval for the
population attributable risk (PAR) or population attributable fraction (PAF)
using a bootstrap approach. The function accounts for stratification by
standardization groups.
}
\examples{
# Example usage:
data <- data.frame(
  exposure = c(1, 0, 1, 0, 1, 0),
  outcome = c(1, 0, 1, 1, 0, 0),
  group = c("A", "A", "B", "B", "A", "B")
)
result <- calculate_standardized_bootstrap_ci(
  type = "par",
  data = data,
  exposure_col = "exposure",
  outcome_col = "outcome",
  standarisation_col = "group",
  interval = 0.95,
  sample_count = 1000
)
print(result)

}
